public class Connection {
    City cityToConnect;
    Integer distanceInMinutes;
    public Connection(City cityToConnect, Integer distanceInMinutes) {
        this.cityToConnect = cityToConnect;
        this.distanceInMinutes = distanceInMinutes;
    }
}
